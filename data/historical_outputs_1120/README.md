# Historical Outputs 1120

This directory contains the `outputs_1120` run copied from the paper development workspace. It is preserved for auditability, but it is not the primary replication path because its metadata references `roundB_types_merged1113_test.csv`, which was not found under `<LOCAL_SEARCH_ROOT>` during packaging.

Use `data/intermediate_outputs/` and `data/final_tree/` as the main reproducible Round C v4 output set.
